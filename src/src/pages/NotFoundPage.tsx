export default function NotFoundPage() {
    return (
        <div>
            <h1 className="header">
                Page not found
            </h1>
            <p>There is not content here. Use the menu to go to a page that exists</p>
        </div>
    );
}
